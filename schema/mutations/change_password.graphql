mutation ChangePasswordMutation(
    $email: String!
    $currentP: String!
    $newP: String!
    $confirmP: String!
) {
    changePassword(
        email: $email
        currentP: $currentP
        newP: $newP
        confirmP: $confirmP
    )
}
